import { roomInteractions, type RoomInteraction } from "./MujiRoom.js";
import {
  addCapsuleThought,
  capsulePreviewText,
  drawCapsuleThought,
  keptCapsules,
  loadCapsuleMachineState,
  machineCapsules,
  removeCapsuleThought,
  saveCapsuleMachineState,
  setCapsuleStatus,
  type CapsuleAttachment,
  type CapsuleMachineState,
  type CapsuleThought
} from "./CapsuleMachine.js";
import { CapsuleMediaStore } from "./CapsuleMediaStore.js";

type RoomInteractionLike = { id: string };
type CapsuleHost = { activateRoomInteraction?: (interaction: RoomInteractionLike) => void };
type CapsuleScreen = "main" | "result" | "opened" | "kept";

const interaction = { id: "capsule" as never, label: "Capsule Machine", x: 684, y: 450, radius: 52 } satisfies RoomInteraction;

function escapeHtml(value: string): string { return value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/\"/g, "&quot;").replace(/'/g, "&#039;"); }
function formatDate(value: string): string { const date = new Date(value); return new Intl.DateTimeFormat(undefined, { year: "numeric", month: "short", day: "2-digit", hour: "2-digit", minute: "2-digit" }).format(date); }
function ageCopy(value: string, now = new Date()): string { const days = Math.max(0, Math.floor((now.getTime() - new Date(value).getTime()) / 86_400_000)); if (days === 0) return "You left this here today."; if (days === 1) return "You left this here yesterday."; return `You left this here ${days} days ago.`; }
function balls(): string { const tones = ["blue", "pink", "yellow", "sage", "lavender"]; return Array.from({ length: 19 }, (_, index) => `<span class="capsule-ball capsule-ball--${tones[index % tones.length]}" aria-hidden="true"></span>`).join(""); }
function machineMarkup(interactive = false, extraClass = "", enabled = true): string { return `<div class="muji-capsule-machine ${extraClass}" aria-label="Capsule machine"><div class="muji-capsule-machine__glass" aria-hidden="true"><div class="muji-capsule-machine__balls">${balls()}</div></div><div class="muji-capsule-machine__neck" aria-hidden="true"></div><div class="muji-capsule-machine__body"><div class="muji-capsule-machine__chute" aria-hidden="true"><span></span></div>${interactive ? `<button class="muji-capsule-machine__knob capsule-knob-button" type="button" data-capsule-action="turn" ${enabled ? "" : "disabled"} aria-label="Turn the capsule machine knob"><i></i></button>` : `<div class="muji-capsule-machine__knob" aria-hidden="true"><i></i></div>`}</div></div>`; }
function attachmentChip(attachment: CapsuleAttachment, removable = false): string { const icon = attachment.kind === "image" ? "▧" : attachment.kind === "video" ? "▶" : "♪"; return `<span class="capsule-media-chip"><b>${icon}</b><span>${escapeHtml(attachment.name || attachment.kind)}</span>${removable ? `<button type="button" data-capsule-action="remove-media" data-capsule-media-remove="${escapeHtml(attachment.id)}" aria-label="Remove attachment">×</button>` : ""}</span>`; }
function mediaViewer(attachments: CapsuleAttachment[] = []): string { if (!attachments.length) return ""; return `<div class="capsule-media-viewer">${attachments.map((attachment) => `<figure class="capsule-media-item capsule-media-item--${attachment.kind}" data-capsule-media-id="${escapeHtml(attachment.id)}" data-capsule-media-kind="${attachment.kind}"><div class="capsule-media-loading">${attachment.kind === "audio" ? "♪ Voice note" : attachment.kind === "video" ? "▶ Video" : "▧ Photo"}</div></figure>`).join("")}</div>`; }
function mainMarkup(state: CapsuleMachineState, draft: string, staged: CapsuleAttachment[], recording: boolean, notice = ""): string { const count = machineCapsules(state).length; const kept = keptCapsules(state).length; return `<div class="capsule-page capsule-page--main"><header class="capsule-header"><button class="capsule-icon-button" type="button" data-capsule-action="close" aria-label="Back to Muji Room">←</button><div class="capsule-title-block"><span class="capsule-eyebrow">THINGS MUJI KEPT</span><h1>Muji Capsule <em>✦</em></h1><p>Write it down, turn the knob, meet it again.</p></div><button class="capsule-kept-button" type="button" data-capsule-action="kept" aria-label="Open kept capsules"><span class="capsule-kept-icon">◒</span><span>Kept</span>${kept ? `<b>${kept}</b>` : ""}</button></header><section class="capsule-compose" aria-label="Leave a thought"><div class="capsule-mascots" aria-hidden="true"><span>◡̈</span><span>•ᴗ•</span><span>˙ᵕ˙</span><span>ᵔᴥᵔ</span><span>⌒‿⌒</span></div><div class="capsule-input-row"><span class="capsule-spark">✦</span><textarea rows="1" data-capsule-input placeholder="What crossed your mind just now?">${escapeHtml(draft)}</textarea><button type="button" data-capsule-action="add" aria-label="Put thought in a capsule">→</button></div><div class="capsule-compose-meta"><span data-capsule-count>${draft.length} characters</span><small>No word limit. Leave as much or as little as you want.</small></div><div class="capsule-media-tools" aria-label="Add something to this capsule"><input type="file" data-capsule-file accept="image/*,video/*" multiple hidden><button type="button" data-capsule-action="pick-media" class="capsule-media-tool capsule-media-tool--gallery"><span class="capsule-media-tool__icon">▧</span><span><strong>Photo / video</strong><small>Add a little scene</small></span></button><button type="button" data-capsule-action="record" class="capsule-media-tool capsule-media-tool--voice ${recording ? "is-recording" : ""}"><span class="capsule-media-tool__icon">${recording ? "■" : "●"}</span><span><strong>${recording ? "Stop recording" : "Voice note"}</strong><small>${recording ? "Tap when you're done" : "Leave your voice here"}</small></span></button></div>${staged.length ? `<div class="capsule-media-staged">${staged.map((item) => attachmentChip(item, true)).join("")}</div>` : ""}</section><section class="capsule-machine-stage">${notice ? `<div class="capsule-notice" role="status">${escapeHtml(notice)}</div>` : ""}${machineMarkup(true, "", count > 0)}<p class="capsule-turn-hint">${count ? "Turn the knob to draw a random thought" : "Leave a thought first, then the knob will wake up"}</p></section><footer class="capsule-footer"><span>${count}</span> ${count === 1 ? "thought is" : "thoughts are"} waiting quietly inside.</footer></div>`; }
function resultMarkup(thought: CapsuleThought): string { return `<div class="capsule-page capsule-page--result"><header class="capsule-simple-header"><button class="capsule-icon-button" type="button" data-capsule-action="main" aria-label="Back">←</button><div><span>CAPSULE DRAW</span><h2>A capsule dropped.</h2></div></header><div class="capsule-result-stage">${machineMarkup(false, "is-drawn")}<div class="capsule-dropped" aria-hidden="true"><i></i></div><div class="capsule-result-sparkles" aria-hidden="true"><i>✦</i><i>·</i><i>✧</i><i>·</i><i>✦</i></div><p>Something from another day found its way back.</p><button class="capsule-primary" type="button" data-capsule-action="open">Open it gently</button></div><span class="capsule-result-id">${escapeHtml(thought.id.slice(-6))}</span></div>`; }
function openedMarkup(thought: CapsuleThought): string { return `<div class="capsule-page capsule-page--opened"><header class="capsule-simple-header"><button class="capsule-icon-button" type="button" data-capsule-action="main" aria-label="Back">←</button><div><span>FROM YOUR PAST SELF</span><h2>Your thought</h2></div></header><div class="capsule-opening-scene" aria-hidden="true"><div class="capsule-opened-visual"><span></span><i></i></div><div class="capsule-opening-glow"></div><div class="capsule-opening-stars"><i>✦</i><i>✧</i><i>·</i><i>✦</i><i>·</i></div></div><article class="capsule-note-card"><time>${escapeHtml(formatDate(thought.createdAt))}</time>${thought.text ? `<p>${escapeHtml(thought.text).replace(/\n/g, "<br>")}</p>` : ""}${mediaViewer(thought.attachments)}<small>${escapeHtml(ageCopy(thought.createdAt))}</small></article><div class="capsule-open-actions"><button type="button" data-capsule-action="put-back"><span>↻</span><strong>Put back</strong><small>Let it surprise you again.</small></button><button type="button" data-capsule-action="keep"><span>♡</span><strong>Keep</strong><small>Move it out of the draw pool.</small></button></div></div>`; }
function keptMarkup(state: CapsuleMachineState): string { const items = keptCapsules(state); return `<div class="capsule-page capsule-page--kept"><header class="capsule-simple-header capsule-simple-header--kept"><button class="capsule-icon-button" type="button" data-capsule-action="main" aria-label="Back">←</button><div><span>YOUR LITTLE DRAWER</span><h2>Kept capsules</h2></div></header><p class="capsule-kept-intro">A quiet preview of what you chose to keep. Open one only when you want the whole thing back.</p><div class="capsule-kept-list">${items.length ? items.map((thought, index) => `<article class="capsule-kept-card capsule-kept-card--preview" data-capsule-action="view-kept" data-capsule-id="${escapeHtml(thought.id)}" tabindex="0" role="button" aria-label="Open kept capsule"><span class="capsule-kept-orb capsule-kept-orb--${index % 5}"></span><div class="capsule-kept-preview-copy"><time>${escapeHtml(formatDate(thought.createdAt))}</time><p>${escapeHtml(capsulePreviewText(thought))}</p>${thought.attachments?.length ? `<div class="capsule-kept-media-summary">${thought.attachments.map((item) => attachmentChip(item)).join("")}</div>` : ""}</div><div class="capsule-kept-card-actions"><span class="capsule-kept-open">Open ›</span><button type="button" data-capsule-action="return" data-capsule-id="${escapeHtml(thought.id)}">Return</button><button type="button" class="capsule-kept-remove" data-capsule-action="remove-kept" data-capsule-id="${escapeHtml(thought.id)}">Remove</button></div></article>`).join("") : `<div class="capsule-empty-kept"><div>◒</div><strong>Nothing kept yet.</strong><p>When a capsule comes back at the right time, you can keep it here.</p></div>`}</div></div>`; }
function mediaKindFor(file: File): "image" | "video" | null { if (file.type.startsWith("image/")) return "image"; if (file.type.startsWith("video/")) return "video"; return null; }
function makeAttachment(blob: Blob, kind: "image" | "video" | "audio", name: string): CapsuleAttachment { const id = typeof crypto !== "undefined" && "randomUUID" in crypto ? crypto.randomUUID() : `capsule-media-${Date.now()}-${Math.random().toString(36).slice(2)}`; return { id, kind, name, mimeType: blob.type || "application/octet-stream", size: blob.size }; }
function preferredAudioMimeType(): string | undefined { if (typeof MediaRecorder === "undefined" || typeof MediaRecorder.isTypeSupported !== "function") return undefined; return ["audio/webm;codecs=opus", "audio/webm", "audio/ogg;codecs=opus"].find((type) => MediaRecorder.isTypeSupported(type)); }

function openCapsuleMachine(): void {
  document.querySelector(".capsule-experience")?.remove();
  let state = loadCapsuleMachineState(localStorage); let screen: CapsuleScreen = "main"; let activeThought: CapsuleThought | null = null; let draft = ""; let staged: CapsuleAttachment[] = []; let timer = 0; let recorder: MediaRecorder | null = null; let recordingStream: MediaStream | null = null; let recordingChunks: Blob[] = []; const mediaStore = new CapsuleMediaStore();
  const shell = document.createElement("section"); shell.className = "capsule-experience"; shell.setAttribute("role", "dialog"); shell.setAttribute("aria-modal", "true"); shell.setAttribute("aria-label", "Muji Capsule Machine");
  const fullscreenHost = document.fullscreenElement instanceof HTMLElement ? document.fullscreenElement : null;
  (fullscreenHost ?? document.body).append(shell);
  document.documentElement.classList.add("capsule-experience-open");
  const persist = (): void => saveCapsuleMachineState(localStorage, state);
  const hydrateMedia = async (): Promise<void> => { const items = [...shell.querySelectorAll<HTMLElement>("[data-capsule-media-id]")]; await Promise.all(items.map(async (host) => { const id = host.dataset.capsuleMediaId; const kind = host.dataset.capsuleMediaKind; if (!id || !kind) return; const url = await mediaStore.objectUrl(id); if (!url || !host.isConnected) return; if (kind === "image") host.innerHTML = `<img src="${url}" alt="Capsule photo">`; if (kind === "video") host.innerHTML = `<video src="${url}" controls playsinline preload="metadata"></video>`; if (kind === "audio") host.innerHTML = `<audio src="${url}" controls preload="metadata"></audio>`; })); };
  const render = (notice = ""): void => { shell.dataset.screen = screen; shell.innerHTML = screen === "main" ? mainMarkup(state, draft, staged, Boolean(recorder), notice) : screen === "result" && activeThought ? resultMarkup(activeThought) : screen === "opened" && activeThought ? openedMarkup(activeThought) : keptMarkup(state); if (screen === "opened") void hydrateMedia(); };
  const stopRecordingTracks = (): void => { recordingStream?.getTracks().forEach((track) => track.stop()); recordingStream = null; };
  const discardStaged = async (): Promise<void> => { const pending = staged; staged = []; await Promise.all(pending.map((item) => mediaStore.delete(item.id).catch(() => undefined))); };
  const close = (): void => { window.clearTimeout(timer); if (recorder?.state === "recording") recorder.stop(); stopRecordingTracks(); void discardStaged(); mediaStore.revokeAll(); shell.remove(); document.documentElement.classList.remove("capsule-experience-open"); window.removeEventListener("keydown", onKeydown); };
  const onKeydown = (event: KeyboardEvent): void => { if (event.key === "Escape") close(); const focused = document.activeElement as HTMLElement | null; if ((event.key === "Enter" || event.key === " ") && focused?.matches(".capsule-kept-card--preview")) focused.click(); }; window.addEventListener("keydown", onKeydown);
  shell.addEventListener("input", (event) => { const target = event.target as HTMLTextAreaElement; if (!target.matches("[data-capsule-input]")) return; draft = target.value; const counter = shell.querySelector<HTMLElement>("[data-capsule-count]"); if (counter) counter.textContent = `${draft.length} characters`; target.style.height = "auto"; target.style.height = `${Math.min(180, target.scrollHeight)}px`; });
  shell.addEventListener("change", (event) => { const input = event.target as HTMLInputElement; if (!input.matches("[data-capsule-file]")) return; void (async () => { for (const file of [...(input.files ?? [])]) { const kind = mediaKindFor(file); if (!kind) continue; const attachment = makeAttachment(file, kind, file.name); await mediaStore.put(attachment.id, file); staged = [...staged, attachment]; } render(staged.length ? "Added to this capsule." : "That file type is not supported here."); })(); });
  shell.addEventListener("click", (event) => { const target = (event.target as HTMLElement).closest<HTMLElement>("[data-capsule-action]"); const action = target?.dataset.capsuleAction; if (!action) return; if (action === "close") return close(); if (action === "main") { screen = "main"; activeThought = null; render(); return; } if (action === "kept") { screen = "kept"; render(); return; } if (action === "pick-media") { shell.querySelector<HTMLInputElement>("[data-capsule-file]")?.click(); return; } if (action === "remove-media") { const id = target.dataset.capsuleMediaRemove; if (!id) return; staged = staged.filter((item) => item.id !== id); void mediaStore.delete(id); render(); return; } if (action === "record") { if (recorder?.state === "recording") { recorder.stop(); return; } if (!navigator.mediaDevices?.getUserMedia || typeof MediaRecorder === "undefined") { render("Voice recording is not available in this browser."); return; } void (async () => { try { recordingStream = await navigator.mediaDevices.getUserMedia({ audio: { channelCount: { ideal: 1 }, sampleRate: { ideal: 48000 }, echoCancellation: true, noiseSuppression: true, autoGainControl: true } }); recordingChunks = []; const mimeType = preferredAudioMimeType(); recorder = new MediaRecorder(recordingStream, { ...(mimeType ? { mimeType } : {}), audioBitsPerSecond: 128000 }); recorder.addEventListener("dataavailable", (chunk) => { if (chunk.data.size) recordingChunks.push(chunk.data); }); recorder.addEventListener("stop", () => { void (async () => { const recordedMimeType = recorder?.mimeType || mimeType || "audio/webm"; const blob = new Blob(recordingChunks, { type: recordedMimeType }); const attachment = makeAttachment(blob, "audio", `Voice note ${new Date().toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" })}`); await mediaStore.put(attachment.id, blob); staged = [...staged, attachment]; recorder = null; stopRecordingTracks(); render("Voice note tucked into this capsule."); })(); }); recorder.start(); render("Recording… tap again when you're done."); } catch { recorder = null; stopRecordingTracks(); render("Microphone permission was not available."); } })(); return; } if (action === "add") { const next = addCapsuleThought(state, draft, new Date(), undefined, staged); if (next === state) { shell.querySelector<HTMLTextAreaElement>("[data-capsule-input]")?.focus(); render("Write something, add a photo/video, or record a voice note first."); return; } state = next; staged = []; draft = ""; persist(); render("Your thought slipped into a capsule."); return; } if (action === "turn") { const result = drawCapsuleThought(state); if (!result) return; state = result.state; activeThought = result.thought; persist(); shell.classList.add("is-turning"); const reduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches; timer = window.setTimeout(() => { shell.classList.remove("is-turning"); screen = "result"; render(); }, reduced ? 80 : 900); return; } if (action === "open" && activeThought) { screen = "opened"; render(); return; } if (action === "put-back" && activeThought) { state = setCapsuleStatus(state, activeThought.id, "machine"); persist(); screen = "main"; activeThought = null; render("Put back. Maybe it will find you another day."); return; } if (action === "keep" && activeThought) { state = setCapsuleStatus(state, activeThought.id, "kept"); persist(); screen = "kept"; activeThought = null; render(); return; } if (action === "view-kept") { const id = target.dataset.capsuleId; const thought = state.thoughts.find((item) => item.id === id); if (!thought) return; activeThought = thought; screen = "opened"; render(); return; } if (action === "return") { event.stopPropagation(); const id = target.dataset.capsuleId; if (!id) return; state = setCapsuleStatus(state, id, "machine"); persist(); render(); return; } if (action === "remove-kept") { event.stopPropagation(); const id = target.dataset.capsuleId; if (!id) return; const thought = state.thoughts.find((item) => item.id === id && item.status === "kept"); if (!thought) return; if (!window.confirm("Remove this kept capsule permanently?")) return; state = removeCapsuleThought(state, id); persist(); void Promise.all((thought.attachments ?? []).map((item) => mediaStore.delete(item.id).catch(() => undefined))); render(); } });
  render();
}

export function installCapsuleMachineBridge(prototype: object): void { if (!roomInteractions.some((item) => String(item.id) === "capsule")) roomInteractions.push(interaction); const marker = prototype as CapsuleHost & { __capsuleMachineInstalled?: boolean }; if (marker.__capsuleMachineInstalled) return; marker.__capsuleMachineInstalled = true; const original = marker.activateRoomInteraction; if (!original) return; marker.activateRoomInteraction = function (interactionItem: RoomInteractionLike): void { if (interactionItem.id === "capsule") { openCapsuleMachine(); return; } original.call(this, interactionItem); }; }
