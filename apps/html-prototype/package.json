{
  "name": "@walk/html-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && node scripts/dev-server.mjs",
    "build": "tsc -p tsconfig.json && node scripts/build.mjs",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {}
}
