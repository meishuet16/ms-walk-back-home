#!/usr/bin/env node
const { spawnSync } = require("node:child_process");

const result = spawnSync(process.execPath, ["scripts/pre-push.mjs"], { stdio: "inherit" });
process.exit(result.status ?? 1);
