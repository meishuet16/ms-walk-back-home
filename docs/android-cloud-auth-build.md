# Android cloud build command

Use `npm run android:sync:cloud` when the APK should expose Google sign-in and Supabase sync. Use the existing `npm run android:sync` for a deliberately local-only build.
